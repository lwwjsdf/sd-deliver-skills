---
name: tracking-plan-design
version: 1.0.0
description: |
  埋点方案设计知识。从业务目标确认到输出埋点方案 Excel（含 Events/Details/Users 三个 sheet）。
  当讨论埋点方案设计、事件定义、属性设计、业务目标确认时自动加载。
  上游知识：业务范围确认（business-scoping）。下游交付给数据管道（sd-tracking-pipeline）。
allowed-tools:
  - Bash
  - Read
  - Write
---

## 核心原则

**Iron Law: 必须先完成采集方案设计并经客户确认，再进行后续任何步骤。**

不允许在方案未确认的情况下造数或创建看板。违反此原则会导致返工。

## 设计流程

### Phase 1：业务目标确认

收集以下信息后才能进入方案设计：
- 核心业务场景（用户注册/购买/使用路径等）
- 需要回答的关键业务问题（转化率？留存？活跃度？）
- 分析维度（渠道、用户分群、时间粒度）
- 数据消费方（运营/产品/数据分析师）

输出：业务目标确认单（一页，列出以上四项）

### Phase 2：采集方案设计

基于业务目标，设计埋点方案：
- 事件列表（事件名、中文名、触发时机）
- 每个事件的属性列表（属性名、类型、示例值、是否必填）
- 用户属性列表（属性名、类型）

输出：埋点方案表（Excel，包含 Events / Details（Event） / Users 三个 sheet）

## Quality Check

- 「数据结构」整理属性时需要确认用于看板查询/筛选的字段补充到该事件属性|用户属性
- 「前置事件」指进入本事件的前置条件，可为空；当某个事件在特定场景才有触发时补充
- 「需在 Excle 中事先穷举的值」对于有限的属性值，减少后续因属性新增导致的改版成本
- 「其他说明」属性的一般说明、默认值（示例数据默认填充）、默认值默认展示的合理性

## 常见问题

**客户说"先做再确认"：** 坚持确认门。方案变更后造数和看板全部作废，返工成本远高于等待确认的时间。

**找不到埋点方案文档：** 使用本 skill 重新设计。

**数据量为零：** 先确认测试设备是否在神策的设备白名单内，再确认 SDK 初始化是否成功。
